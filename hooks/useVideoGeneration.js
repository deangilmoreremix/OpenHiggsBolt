/**
 * Root-level hook wrapper for VFX video generation.
 * Re-exports the production hook from src/hooks.
 */

export { useVideoGeneration } from '../src/hooks/useVideoGeneration'
export { useVideoGeneration as default } from '../src/hooks/useVideoGeneration'
