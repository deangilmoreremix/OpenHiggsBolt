import StandaloneShell from '@/components/StandaloneShell';

export const metadata = {
  title: 'Workflow — SmartVideo GO',
};

export default function WorkflowPage() {
  return <StandaloneShell />;
}
