# Server-side only. Never expose in the browser.
FIRECRAWL_API_KEY=

# Optional override for the OpenAI model used in asset classification.
# Defaults to gpt-4o-mini if unset.
OPENAI_ASSET_CLASSIFICATION_MODEL=
